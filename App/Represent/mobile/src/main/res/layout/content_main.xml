<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="io.github.kathyyliang.represent.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/instruction"
        android:id="@+id/textView" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/zip_code"
        android:layout_marginTop="100dp"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:hint="@string/zip_code"
        android:textAlignment="center"
        android:backgroundTint="@color/colorAccent" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/zip_code_button"
        android:id="@+id/zip_code_button"
        android:textColor="@color/colorPrimary"
        android:layout_below="@+id/zip_code"
        android:layout_marginTop="15dp"
        android:layout_alignEnd="@+id/zip_code"
        android:layout_alignStart="@+id/zip_code"
        android:backgroundTint="@color/colorAccent" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/location_button"
        android:id="@+id/location_button"
        android:textColor="@color/colorPrimary"
        android:layout_marginTop="75dp"
        android:backgroundTint="@color/colorAccent"
        android:layout_below="@+id/zip_code_button"
        android:layout_alignStart="@+id/zip_code_button"
        android:layout_alignEnd="@+id/zip_code_button" />

</RelativeLayout>
